{
  "name": "container-ui",
  "version": "0.1.0",
  "description": "Web UI for managing container images via the docker/crictl CLI.",
  "type": "module",
  "bin": {
    "container-ui": "server.js"
  },
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=24"
  },
  "license": "MIT",
  "private": true
}
